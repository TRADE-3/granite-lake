<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- WifiManager.isWifiEnabled() (MainActivity.kt's isWifiRadioOn,
         backing ConnectivityHeuristicService.isWifiRadioOn) requires this -
         confirmed on-device: without it, every call throws
         SecurityException, which the fail-open catch then silently turned
         into "assume Wi-Fi is on" regardless of the real toggle state. A
         normal (not dangerous) permission, granted at install time, no
         runtime prompt. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <!-- Offline-capture design doc §7.2: the background reconnect watchdog
         (reconnect_notification_service.dart) needs this to post its
         "unlock to submit" reminder on Android 13+. Requested at runtime
         (ReconnectNotificationService.initialize), not just declared here. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Lets the capture screen distinguish "Mobile Data toggle off" and
         "mobile radio truly off" from "on but no signal" via ServiceState -
         connectivity_plus alone can't tell those apart. Feeds the
         soft connectivity-rule check (Wi-Fi on / airplane off / Mobile
         Data on if a SIM exists) used for advisory messaging and
         is_forced_offline attribution, never a hard block. Requested at
         runtime, at most once per process (MainActivity.kt's
         getMobilePhoneState), never upfront during onboarding. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- The app never records audio or requests legacy shared-storage read
         access; camera_android_camerax (and a transitive dependency) merge
         these in for capabilities this app doesn't use. Removed rather than
         granted: neither is ever requested at runtime, so leaving them in
         only inflates the app's declared permission surface. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <application
        android:label="Trade3"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
